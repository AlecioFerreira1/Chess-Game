#pragma once
#include <string>

namespace Chess::Config{
  const std::string windowTitle = "Xadrez";
  constexpr int windowWidth = 900;
  constexpr int windowHeight = 900; 
  static inline bool enableAutoFlip = true; 
  static inline bool soundEnabled = true;
}